name: lsa_profile_verification
description: HabotConnect Flutter hiring project - LSA Profile Verification demo.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3
  http: ^1.6.0
  crypto: ^3.0.7
  uuid: ^4.6.0
  shared_preferences: ^2.5.5
  shared_preferences_platform_interface: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
